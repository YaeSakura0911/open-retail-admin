import { YsInputDirective } from './ys-input.directive';

describe('YsInputDirective', () => {
  it('should create an instance', () => {
    const directive = new YsInputDirective();
    expect(directive).toBeTruthy();
  });
});
